import eslint from "@eslint/js";
import { defineConfig } from "eslint/config";
import tseslint from "typescript-eslint";

export default defineConfig(
  eslint.configs.recommended,
  tseslint.configs.recommendedTypeChecked,
  tseslint.configs.stylisticTypeChecked,
  {
    languageOptions: {
      parserOptions: {
        projectService: true,
      },
    },
  },

  {
    rules: {
      // don't allow @ts-ignore, ... without comments
      "@typescript-eslint/ban-ts-comment": [
        "error",
        {
          minimumDescriptionLength: 3,
          "ts-check": false,
          "ts-expect-error": "allow-with-description",
          "ts-ignore": true, // use @ts-expect-error
          "ts-nocheck": "allow-with-description",
        },
      ],

      // allow @tslint comments
      // '@typescript-eslint/ban-tslint-comment': 'off',

      // require function return types
      "@typescript-eslint/explicit-function-return-type": [
        "warn",
        {
          allowExpressions: true,
        },
      ],

      // don't allow usage / returning of void expressions
      "@typescript-eslint/no-confusing-void-expression": [
        "error",
        {
          ignoreArrowShorthand: true, // allow in arrow functions: () => console.log(), ...
        },
      ],

      // allow redundant types for readability (err: Error | unknown, ...)
      "@typescript-eslint/no-redundant-type-constituents": "off",

      // require const enums
      "no-restricted-syntax": [
        "warn",
        {
          selector: "TSEnumDeclaration:not([const=true])",
          message: "Use const enums instead.",
        },
      ],

      // don't allow unused vars unless they start with _
      "no-unused-vars": "off",
      "@typescript-eslint/no-unused-vars": [
        "warn",
        {
          args: "all",
          argsIgnorePattern: "^_",
          caughtErrors: "all",
          caughtErrorsIgnorePattern: "^_",
          destructuredArrayIgnorePattern: "^_",
          varsIgnorePattern: "^_",
          ignoreRestSiblings: true,
        },
      ],

      // reminder to remove temp comments
      "no-warning-comments": [
        "warn",
        {
          terms: ["@temp"],
          location: "anywhere",
        },
      ],

      // allow OR for strings
      "@typescript-eslint/prefer-nullish-coalescing": [
        "warn",
        {
          ignorePrimitives: {
            string: true,
          },
        },
      ],

      // require functions returning promises to be marked async (simplifies error handling)
      "@typescript-eslint/promise-function-async": "warn",
    },
  },
);
