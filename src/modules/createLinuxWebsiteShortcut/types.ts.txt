interface TaskbarTabs_JSON {
  version: number; // 1
  taskbarTabs: TaskbarTab[];
}

export interface TaskbarTab {
  id: string; // uuid
  scopes: Scope[];
  userContextId: number; // 0 ?
  startUrl: string;
  name: string;
}

export interface Scope {
  hostname: string;
  prefix?: string; // '/' in case of yt sometimes ?
}
