import type { Module } from "../../types.ts";

export default {
  name: "createLinuxBrowserShortcut",
  description: "Creates ",
  arguments: undefined,
  run: (_args) => {
    return 0;
  },
} satisfies Module<undefined>;
